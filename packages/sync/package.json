{
  "name": "@tsukumo/sync",
  "version": "0.1.0",
  "description": "tsukumo sync: replicates rules across nodes via Automerge (the only tsukumo package with dependencies)",
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=22.18"
  },
  "scripts": {
    "start": "node src/index.ts",
    "test": "node --test test/*.test.ts"
  },
  "dependencies": {
    "@automerge/automerge-repo": "^2.0.0",
    "@automerge/automerge-repo-network-websocket": "^2.0.0",
    "@automerge/automerge-repo-storage-nodefs": "^2.0.0",
    "ws": "^8.18.0"
  }
}
